{
  "name": "threads-typescript",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -p packages/core --noEmit && tsc -p packages/adapter-testkit --noEmit && tsc -p packages/anthropic --noEmit && tsc -p packages/openai --noEmit && tsc -p packages/ai-sdk --noEmit",
    "lint": "biome check --error-on-warnings .",
    "test": "bun test",
    "check": "bun run typecheck && bun run lint && bun run test && bun run schema:check",
    "schema:export": "bun packages/core/scripts/export-schema.ts",
    "schema:check": "bun packages/core/scripts/export-schema.ts --check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
