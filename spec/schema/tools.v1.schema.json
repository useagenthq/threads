{
  "$defs": {
    "AskInput": {
      "additionalProperties": false,
      "properties": {
        "question": {
          "minLength": 1,
          "type": "string"
        },
        "to": {
          "description": "A member's name, such as researcher-1.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "to",
        "question"
      ],
      "type": "object"
    },
    "BashInput": {
      "additionalProperties": false,
      "properties": {
        "command": {
          "description": "Run with bash -c.",
          "minLength": 1,
          "type": "string"
        },
        "timeout_ms": {
          "description": "Default 120000.",
          "maximum": 9007199254740991,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "command"
      ],
      "type": "object"
    },
    "CancelInput": {
      "additionalProperties": false,
      "properties": {
        "member": {
          "description": "A member's name, such as researcher-1.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "member"
      ],
      "type": "object"
    },
    "ComputerInput": {
      "additionalProperties": false,
      "properties": {
        "action": {
          "enum": [
            "click",
            "double_click",
            "right_click",
            "move",
            "drag",
            "scroll",
            "type",
            "key"
          ],
          "type": "string"
        },
        "amount": {
          "description": "scroll clicks; default 3.",
          "maximum": 50,
          "minimum": 1,
          "type": "integer"
        },
        "direction": {
          "description": "scroll direction.",
          "enum": [
            "up",
            "down",
            "left",
            "right"
          ],
          "type": "string"
        },
        "text": {
          "description": "type: the text. key: a combination such as ctrl+s.",
          "minLength": 1,
          "type": "string"
        },
        "to_x": {
          "description": "drag: the end point.",
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        },
        "to_y": {
          "description": "drag: the end point.",
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        },
        "x": {
          "description": "Screenshot pixels: required for click, double_click, right_click, move, drag and scroll.",
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        },
        "y": {
          "description": "Screenshot pixels, with x.",
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "action"
      ],
      "type": "object"
    },
    "ComputerScreenshotInput": {
      "additionalProperties": false,
      "properties": {
        "to_x": {
          "description": "Zoom: the region's right edge.",
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        },
        "to_y": {
          "description": "Zoom: the region's bottom edge.",
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        },
        "wait_ms": {
          "description": "Wait this long before capturing.",
          "maximum": 60000,
          "minimum": 1,
          "type": "integer"
        },
        "x": {
          "description": "Zoom: the region's left edge; give all four or none.",
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        },
        "y": {
          "description": "Zoom: the region's top edge.",
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "EditInput": {
      "additionalProperties": false,
      "properties": {
        "expected_sha256": {
          "description": "The file's current lowercase hex sha256; a mismatch changes nothing.",
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "new_string": {
          "type": "string"
        },
        "old_string": {
          "minLength": 1,
          "type": "string"
        },
        "path": {
          "description": "Relative to /workspace, or absolute.",
          "minLength": 1,
          "type": "string"
        },
        "replace_all": {
          "type": "boolean"
        }
      },
      "required": [
        "path",
        "old_string",
        "new_string"
      ],
      "type": "object"
    },
    "ForgetMemoryInput": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "The id of a recalled memory.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "GitCloneInput": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "description": "The clone's directory, relative to /workspace; default the repo name.",
          "minLength": 1,
          "type": "string"
        },
        "ref": {
          "description": "Branch, tag or commit; default the default branch.",
          "minLength": 1,
          "type": "string"
        },
        "repo": {
          "description": "owner/name on the configured forge.",
          "pattern": "^[A-Za-z0-9_.-]+[/][A-Za-z0-9_.-]+$",
          "type": "string"
        }
      },
      "required": [
        "repo"
      ],
      "type": "object"
    },
    "GitFetchInput": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "description": "The clone's directory, relative to /workspace; default the repo name.",
          "minLength": 1,
          "type": "string"
        },
        "ref": {
          "description": "Fetch only this ref; default every branch.",
          "minLength": 1,
          "type": "string"
        },
        "repo": {
          "description": "owner/name on the configured forge.",
          "pattern": "^[A-Za-z0-9_.-]+[/][A-Za-z0-9_.-]+$",
          "type": "string"
        }
      },
      "required": [
        "repo"
      ],
      "type": "object"
    },
    "GitPushInput": {
      "additionalProperties": false,
      "properties": {
        "branch": {
          "description": "The local branch; it is pushed to the same name.",
          "minLength": 1,
          "type": "string"
        },
        "path": {
          "description": "The clone's directory, relative to /workspace; default the repo name.",
          "minLength": 1,
          "type": "string"
        },
        "repo": {
          "description": "owner/name on the configured forge.",
          "pattern": "^[A-Za-z0-9_.-]+[/][A-Za-z0-9_.-]+$",
          "type": "string"
        }
      },
      "required": [
        "repo",
        "branch"
      ],
      "type": "object"
    },
    "GlobInput": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "default": ".",
          "description": "Directory, relative to /workspace; . is the workspace root.",
          "minLength": 1,
          "type": "string"
        },
        "pattern": {
          "description": "A gitignore-style glob relative to path; ** spans directories.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "pattern"
      ],
      "type": "object"
    },
    "GrepInput": {
      "additionalProperties": false,
      "properties": {
        "glob": {
          "description": "Only files whose path relative to path matches this glob.",
          "minLength": 1,
          "type": "string"
        },
        "path": {
          "default": ".",
          "description": "Directory, relative to /workspace; . is the workspace root.",
          "minLength": 1,
          "type": "string"
        },
        "pattern": {
          "description": "An extended regular expression.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "pattern"
      ],
      "type": "object"
    },
    "HandoffInput": {
      "additionalProperties": false,
      "properties": {
        "agent": {
          "description": "A handoff target listed in your instructions.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "agent"
      ],
      "type": "object"
    },
    "LoadSkillInput": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "A skill name from the listing.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "LsInput": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "default": ".",
          "description": "Directory, relative to /workspace; . is the workspace root.",
          "minLength": 1,
          "type": "string"
        }
      },
      "type": "object"
    },
    "LspInput": {
      "additionalProperties": false,
      "properties": {
        "character": {
          "description": "1-based column; required with line.",
          "maximum": 9007199254740991,
          "minimum": 1,
          "type": "integer"
        },
        "line": {
          "description": "1-based; required for definition, references and hover.",
          "maximum": 9007199254740991,
          "minimum": 1,
          "type": "integer"
        },
        "operation": {
          "enum": [
            "diagnostics",
            "definition",
            "references",
            "hover",
            "symbols"
          ],
          "type": "string"
        },
        "path": {
          "description": "Relative to /workspace, or absolute.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "operation",
        "path"
      ],
      "type": "object"
    },
    "MonitorInput": {
      "additionalProperties": false,
      "properties": {
        "member": {
          "description": "A member's name, such as researcher-1.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "member"
      ],
      "type": "object"
    },
    "NotebookEditInput": {
      "additionalProperties": false,
      "properties": {
        "cell_id": {
          "description": "The nbformat cell id; insert adds the new cell after it.",
          "minLength": 1,
          "type": "string"
        },
        "cell_type": {
          "description": "Required for insert; replace keeps the type when absent.",
          "enum": [
            "code",
            "markdown"
          ],
          "type": "string"
        },
        "mode": {
          "default": "replace",
          "enum": [
            "replace",
            "insert",
            "delete"
          ],
          "type": "string"
        },
        "new_source": {
          "description": "The cell's new source; ignored by delete.",
          "type": "string"
        },
        "path": {
          "description": "An .ipynb file, relative to /workspace.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "path",
        "cell_id",
        "new_source"
      ],
      "type": "object"
    },
    "OpenPullRequestInput": {
      "additionalProperties": false,
      "properties": {
        "base": {
          "description": "The branch to merge into.",
          "minLength": 1,
          "type": "string"
        },
        "body": {
          "type": "string"
        },
        "head": {
          "description": "The pushed branch.",
          "minLength": 1,
          "type": "string"
        },
        "repo": {
          "description": "owner/name on the configured forge.",
          "pattern": "^[A-Za-z0-9_.-]+[/][A-Za-z0-9_.-]+$",
          "type": "string"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "repo",
        "head",
        "base",
        "title"
      ],
      "type": "object"
    },
    "ReadInput": {
      "additionalProperties": false,
      "properties": {
        "limit": {
          "default": 2000,
          "description": "Lines to read.",
          "maximum": 9007199254740991,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 1,
          "description": "First line, 1-based.",
          "maximum": 9007199254740991,
          "minimum": 1,
          "type": "integer"
        },
        "path": {
          "description": "Relative to /workspace, or absolute.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "ReadToolResultInput": {
      "additionalProperties": false,
      "properties": {
        "call_id": {
          "minLength": 1,
          "type": "string"
        },
        "length": {
          "maximum": 65536,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "call_id",
        "offset",
        "length"
      ],
      "type": "object"
    },
    "ReplyInput": {
      "additionalProperties": false,
      "properties": {
        "ask_id": {
          "description": "The ask_id of an ask you received.",
          "minLength": 1,
          "type": "string"
        },
        "text": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "ask_id",
        "text"
      ],
      "type": "object"
    },
    "SaveMemoryInput": {
      "additionalProperties": false,
      "properties": {
        "text": {
          "description": "The fact to remember, self-contained.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "SearchKnowledgeInput": {
      "additionalProperties": false,
      "properties": {
        "k": {
          "description": "Most hits to return. Default 5.",
          "maximum": 20,
          "minimum": 1,
          "type": "integer"
        },
        "query": {
          "minLength": 1,
          "type": "string"
        },
        "sources": {
          "description": "Only these source ids.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "SearchMemoryInput": {
      "additionalProperties": false,
      "properties": {
        "k": {
          "description": "Most hits to return. Default 5.",
          "maximum": 20,
          "minimum": 1,
          "type": "integer"
        },
        "query": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "SendInput": {
      "additionalProperties": false,
      "properties": {
        "text": {
          "minLength": 1,
          "type": "string"
        },
        "to": {
          "description": "A member's name, such as researcher-1.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "to",
        "text"
      ],
      "type": "object"
    },
    "SendMessageInput": {
      "additionalProperties": false,
      "properties": {
        "text": {
          "minLength": 1,
          "type": "string"
        },
        "to": {
          "description": "A member's agent name, or * for every member.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "to",
        "text"
      ],
      "type": "object"
    },
    "SpawnAgentInput": {
      "additionalProperties": false,
      "properties": {
        "agent": {
          "description": "A subagent name listed in your instructions.",
          "minLength": 1,
          "type": "string"
        },
        "background": {
          "description": "Default false: wait for the result.",
          "type": "boolean"
        },
        "isolation": {
          "description": "Default none.",
          "enum": [
            "none",
            "shared_sandbox",
            "worktree",
            "forked_sandbox"
          ],
          "type": "string"
        },
        "prompt": {
          "description": "The task; the subagent sees nothing else.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "agent",
        "prompt"
      ],
      "type": "object"
    },
    "StartInput": {
      "additionalProperties": false,
      "properties": {
        "agent": {
          "description": "An agent your team lists.",
          "minLength": 1,
          "type": "string"
        },
        "instructions": {
          "description": "A dynamic agent only: instructions you write for this member, added after its own. Omitted: its own alone.",
          "minLength": 1,
          "type": "string"
        },
        "label": {
          "description": "A short display name for the member, up to 64 characters. Any start; never shown to a model.",
          "minLength": 1,
          "type": "string"
        },
        "model": {
          "description": "A dynamic agent only: one of its model keys. Omitted: its default.",
          "minLength": 1,
          "type": "string"
        },
        "task": {
          "description": "The member's first input; it sees nothing else.",
          "minLength": 1,
          "type": "string"
        },
        "tools": {
          "description": "A dynamic agent only: the tools it may use, a subset of those your instructions list for it. Omitted: all of them.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "agent",
        "task"
      ],
      "type": "object"
    },
    "TeamTaskClaimInput": {
      "additionalProperties": false,
      "properties": {
        "task_id": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "task_id"
      ],
      "type": "object"
    },
    "TeamTaskCreateInput": {
      "additionalProperties": false,
      "properties": {
        "blocked_by": {
          "description": "Task ids that must complete before this one can be claimed.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "description": {
          "type": "string"
        },
        "subject": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "subject"
      ],
      "type": "object"
    },
    "TeamTaskUpdateInput": {
      "additionalProperties": false,
      "properties": {
        "status": {
          "description": "released returns a claimed task to the open pool.",
          "enum": [
            "completed",
            "failed",
            "released"
          ],
          "type": "string"
        },
        "task_id": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "task_id",
        "status"
      ],
      "type": "object"
    },
    "TodoWriteInput": {
      "additionalProperties": false,
      "properties": {
        "todos": {
          "description": "The complete new list; ids are unique.",
          "items": {
            "additionalProperties": false,
            "properties": {
              "active_form": {
                "description": "Present tense, shown while in_progress.",
                "type": "string"
              },
              "content": {
                "minLength": 1,
                "type": "string"
              },
              "id": {
                "minLength": 1,
                "type": "string"
              },
              "status": {
                "enum": [
                  "pending",
                  "in_progress",
                  "completed"
                ],
                "type": "string"
              }
            },
            "required": [
              "id",
              "content",
              "status"
            ],
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "todos"
      ],
      "type": "object"
    },
    "ToolSearchInput": {
      "additionalProperties": false,
      "properties": {
        "limit": {
          "default": 5,
          "description": "How many keyword matches to load.",
          "maximum": 10,
          "minimum": 1,
          "type": "integer"
        },
        "query": {
          "description": "Exact tool names separated by commas, or keywords. At most 200 characters.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "WaitInput": {
      "additionalProperties": false,
      "properties": {
        "members": {
          "items": {
            "description": "A member's name, such as researcher-1.",
            "minLength": 1,
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "members"
      ],
      "type": "object"
    },
    "WebFetchInput": {
      "additionalProperties": false,
      "properties": {
        "url": {
          "description": "An http or https URL, printable ASCII (percent-encode the rest).",
          "pattern": "^https?:[/][/][!-~]+$",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "WebSearchInput": {
      "additionalProperties": false,
      "properties": {
        "allowed_domains": {
          "description": "Hosts; a subdomain of a listed host matches too.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "blocked_domains": {
          "description": "Hosts; a subdomain of a listed host matches too.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "query": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "WriteInput": {
      "additionalProperties": false,
      "properties": {
        "content": {
          "type": "string"
        },
        "expected_sha256": {
          "description": "The file's current lowercase hex sha256; a mismatch changes nothing.",
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "path": {
          "description": "Relative to /workspace, or absolute.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "path",
        "content"
      ],
      "type": "object"
    }
  },
  "$id": "urn:threads:schema:tools:v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Generated from typescript/packages/core/src/tools/catalog.ts by bun run schema:export. Do not edit.",
  "title": "Built-in tool inputs"
}
