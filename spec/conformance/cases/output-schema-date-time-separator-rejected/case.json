{
  "name": "output-schema-date-time-separator-rejected",
  "family": "tools_streaming",
  "kind": "reduce",
  "description": "output_validated{accepted} whose at breaks only the date-time-separator rule of the pinned output schema (spec/schema/README.md, Output schemas): invalid_transition. Every keyword is checked, never skipped.",
  "clock": {
    "now": 1790000060000
  }
}
