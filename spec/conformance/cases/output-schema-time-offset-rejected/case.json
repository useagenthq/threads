{
  "name": "output-schema-time-offset-rejected",
  "family": "tools_streaming",
  "kind": "reduce",
  "description": "output_validated{accepted} whose clock breaks only the time-offset rule of the pinned output schema (spec/schema/README.md, Output schemas): invalid_transition. Every keyword is checked, never skipped.",
  "clock": {
    "now": 1790000060000
  }
}
