---
title: "localMemory / local_memory"
description: "Built-in memory: SQLite FTS5 in the run's store."
icon: "SquareFunction"
---

Refused on a postgres() store (invalid_config naming supermemory() and zep()).

Import from `@threads/core` (TypeScript) or `threads` (Python).

<Tabs items={["TypeScript", "Python"]} groupId="lang" persist>
<Tab value="TypeScript">

```ts
function localMemory(): MemoryProvider
```

</Tab>
<Tab value="Python">

```python
def local_memory() -> MemoryProvider
```

</Tab>
</Tabs>

## Returns

[`MemoryProvider`](/docs/reference/types/MemoryProvider)
