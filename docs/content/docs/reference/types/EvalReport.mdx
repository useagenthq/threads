---
title: "EvalReport"
description: "What runEvals returns and `threads eval --out` writes: per-case results, counts, the one-line summary, model calls and cost."
icon: "Braces"
---

No timestamps, so an offline report is byte-stable in both languages.

<Tabs items={["TypeScript", "Python"]} groupId="lang" persist>
<Tab value="TypeScript">

```ts
type EvalReport = EvalReport;
```

</Tab>
<Tab value="Python">

```python
type EvalReport = EvalReport
```

</Tab>
</Tabs>
