---
title: "Hooks"
description: "Every hook is optional, awaited and bounded by hook_timeout_ms."
icon: "Braces"
---

Decisions are &#123;decision: &lt;hook_decision.decision>, ...&#125; objects. Injections are strings rendered as injected&#123;source: hook&#125;. A gating hook that throws or times out denies.

## Fields

<Field name="sessionStart / session_start" type={"(source: \"startup\" | \"resume\" | \"fork\" | \"compact\", ctx: RunContext<Deps>) => Promise<readonly string[]>"} />

<Field name="sessionEnd / session_end" type={"(ctx: RunContext<Deps>) => Promise<void>"} />

<Field name="beforeInput / before_input" type={"(input: UserInputEvent.data, ctx: RunContext<Deps>) => Promise<{ decision: \"allow\"; injections?: readonly string[] } | { decision: \"deny\"; reason: string }>"} />

<Field name="beforeModel / before_model" type={"(state: ReducedState, ctx: RunContext<Deps>) => Promise<{ decision: \"proceed\"; injections?: readonly string[] } | { decision: \"deny\"; reason: string }>"} />

<Field name="afterModel / after_model" type={"(state: ReducedState, response: ModelResponseEvent.data, ctx: RunContext<Deps>) => Promise<{ decision: \"proceed\" } | { decision: \"deny\"; reason: string } | { decision: \"guide\"; text: string } | { decision: \"retry\"; reason: string }>"} />

<Field name="beforeTool / before_tool" type={"(call: ToolCallEvent.data, ctx: RunContext<Deps>) => Promise<{ decision: \"allow\" } | { decision: \"deny\"; reason: string } | { decision: \"ask\"; rule?: PermissionRule }>"} />

<Field name="permissionRequest / permission_request" type={"(call: ToolCallEvent.data, ctx: RunContext<Deps>) => Promise<{ decision: \"allow\" } | { decision: \"deny\"; reason: string } | { decision: \"ask\"; rule?: PermissionRule }>"} />

<Field name="permissionDenied / permission_denied" type={"(call: ToolCallEvent.data, ctx: RunContext<Deps>) => Promise<void>"} />

<Field name="afterTool / after_tool" type={"(call: ToolCallEvent.data, result: ToolResultEvent.data, ctx: RunContext<Deps>) => Promise<readonly string[]>"} />

<Field name="beforeToolResult / before_tool_result" type={"(call: ToolCallEvent.data, result: ToolResultEvent.data, ctx: RunContext<Deps>) => Promise<{ decision: \"proceed\" } | { decision: \"redact\"; spans: readonly Span[] } | { decision: \"deny\"; reason: string }>"} />

<Field name="afterToolBatch / after_tool_batch" type={"(state: ReducedState, ctx: RunContext<Deps>) => Promise<readonly string[]>"} />

<Field name="beforeCompact / before_compact" type={"(state: ReducedState, ctx: RunContext<Deps>) => Promise<{ decision: \"proceed\" } | { decision: \"deny\"; reason: string } | { decision: \"guide\"; text: string }>"} />

<Field name="afterCompact / after_compact" type={"(state: ReducedState, ctx: RunContext<Deps>) => Promise<readonly string[]>"} />

<Field name="onStop / on_stop" type={"(state: ReducedState, ctx: RunContext<Deps>) => Promise<{ decision: \"stop\" } | { decision: \"continue\"; reason: string }>"} />

<Field name="onStopFailure / on_stop_failure" type={"(code: RunErrorCode, ctx: RunContext<Deps>) => Promise<void>"} />

<Field name="subagentStart / subagent_start" type={"(call: ToolCallEvent.data, ctx: RunContext<Deps>) => Promise<{ decision: \"allow\" } | { decision: \"deny\"; reason: string }>"} />

<Field name="subagentStop / subagent_stop" type={"(finished: AgentFinishedEvent.data, ctx: RunContext<Deps>) => Promise<{ decision: \"stop\" } | { decision: \"continue\"; reason: string }>"} />

<Field name="beforeModelSwitch / before_model_switch" type={"(settings: ModelSettings, ctx: RunContext<Deps>) => Promise<{ decision: \"allow\" } | { decision: \"deny\"; reason: string }>"} />

<Field name="afterModelSwitch / after_model_switch" type={"(settings: ModelSettings, ctx: RunContext<Deps>) => Promise<void>"} />

<Field name="notification" type={"(event: Event, ctx: RunContext<Deps>) => Promise<void>"} />
