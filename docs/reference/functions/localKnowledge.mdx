---
title: "localKnowledge / local_knowledge"
description: "Built-in knowledge: SQLite FTS5 over admitted versions stored as artifacts."
icon: "square-function"
---



Import from `@threads/core` (TypeScript) or `threads` (Python).

<CodeGroup>

```ts TypeScript
function localKnowledge(options: {
  paths: readonly string[];
}): KnowledgeProvider
```

```python Python
def local_knowledge(
    *,
    paths: Sequence[str],
) -> KnowledgeProvider
```

</CodeGroup>

## Parameters

<ParamField body="paths" type={"readonly string[]"} required>
   
</ParamField>

## Returns

[`KnowledgeProvider`](/reference/types/KnowledgeProvider)
