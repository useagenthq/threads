---
title: "sqlite"
description: "The one log and artifact store."
icon: "square-function"
---

Opens lazily: no I/O until first use.

Import from `@threads/core` (TypeScript) or `threads` (Python).

<CodeGroup>

```ts TypeScript
function sqlite(path: string): Store
```

```python Python
def sqlite(
    path: str,
) -> Store
```

</CodeGroup>

## Parameters

<ParamField body="path" type={"string"} required>
  ":memory:" for a throwaway store (tests).
</ParamField>

## Returns

[`Store`](/reference/types/Store)
