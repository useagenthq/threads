---
title: "scriptedModel / scripted_model"
description: "Test kit."
icon: "square-function"
---

Consumes the script in order, deterministic ids; a call past the end fails the run. The exact requests are in the log.

Import from `@threads/core` (TypeScript) or `threads` (Python).

<CodeGroup>

```ts TypeScript
function scriptedModel(script: ModelScript): Model
```

```python Python
def scripted_model(
    script: ModelScript,
) -> Model
```

</CodeGroup>

## Parameters

<ParamField body="script" type={"ModelScript"} required>
   
</ParamField>

## Returns

[`Model`](/reference/types/Model)
