---
title: "ChannelAdapter"
description: "What a channel adapter (slack(), whatsapp(), github()) returns."
icon: "plug"
---

verify, parse, ack and render are pure.

<Note>A protocol: adapters implement it.</Note>

## Properties

<ResponseField name="agent" type={"string"} required>
  The host agent key this channel routes to.
</ResponseField>

<ResponseField name="capabilities" type={"ChannelCapabilities"} required>
   
</ResponseField>

<ResponseField name="limits" type={"Record<string, number>"} required>
  Provider limits (message bytes, rate).
</ResponseField>

<ResponseField name="secrets" type={"Record<string, Secret>"} required>
  The credentials perform needs, by name. The host resolves them at ready() (missing_secret) and passes their values as perform's credentials; they never reach a sandbox, the log or a prompt.
</ResponseField>

## Methods

### verify

<CodeGroup>

```ts TypeScript
verify(raw: RawRequest): Result<VerifiedDelivery>
```

```python Python
def verify(
    raw: RawRequest,
) -> Ok[VerifiedDelivery] | Err[Failure]
```

</CodeGroup>

<ParamField body="raw" type={"RawRequest"} required>
  See [RawRequest](/reference/types/RawRequest).
</ParamField>

**Returns an error value** with one of these codes: `unverified`.

### parse

<CodeGroup>

```ts TypeScript
parse(raw: RawRequest): Result<readonly Inbound[]>
```

```python Python
def parse(
    raw: RawRequest,
) -> Ok[Sequence[Inbound]] | Err[Failure]
```

</CodeGroup>

<ParamField body="raw" type={"RawRequest"} required>
  See [RawRequest](/reference/types/RawRequest).
</ParamField>

**Returns an error value** with one of these codes: `invalid`.

### ack

<CodeGroup>

```ts TypeScript
ack(raw: RawRequest): RawResponse
```

```python Python
def ack(
    raw: RawRequest,
) -> RawResponse
```

</CodeGroup>

<ParamField body="raw" type={"RawRequest"} required>
  See [RawRequest](/reference/types/RawRequest).
</ParamField>

### challenge

A provider's GET subscription check on the webhook URL (WhatsApp hub.mode/hub.verify_token/hub.challenge). Pure: compares the token with the adapter's secret in constant time. unverified answers 401 and nothing is stored.

<CodeGroup>

```ts TypeScript
challenge(query: Record<string, string>): Result<RawResponse>
```

```python Python
def challenge(
    query: Mapping[str, str],
) -> Ok[RawResponse] | Err[Failure]
```

</CodeGroup>

<ParamField body="query" type={"Record<string, string>"} required>
   
</ParamField>

**Returns an error value** with one of these codes: `unverified`.

### render

Outbound ops (adapter-defined JSON).

<CodeGroup>

```ts TypeScript
render(event: Event): readonly JsonObject[]
```

```python Python
def render(
    event: Event,
) -> Sequence[JsonObject]
```

</CodeGroup>

<ParamField body="event" type={"Event"} required>
   
</ParamField>

### perform

<CodeGroup>

```ts TypeScript
perform(op: JsonObject, effectKey: string, credentials: Record<string, string>): Promise<DeliveryOutcome>
```

```python Python
async def perform(
    op: JsonObject,
    effect_key: str,
    credentials: Mapping[str, str],
) -> DeliveryOutcome
```

</CodeGroup>

<ParamField body="op" type={"JsonObject"} required>
   
</ParamField>

<ParamField body="effectKey / effect_key" type={"string"} required>
   
</ParamField>

<ParamField body="credentials" type={"Record<string, string>"} required>
   
</ParamField>

### lookup

found carries the platform_ref. op is the op perform was given for this key (from its recorded tool_call), since a platform lookup is scoped to its conversation.

<CodeGroup>

```ts TypeScript
lookup(effectKey: string, op: JsonObject): Promise<LookupResult<string>>
```

```python Python
async def lookup(
    effect_key: str,
    op: JsonObject,
) -> LookupResult[str]
```

</CodeGroup>

<ParamField body="effectKey / effect_key" type={"string"} required>
   
</ParamField>

<ParamField body="op" type={"JsonObject"} required>
   
</ParamField>
