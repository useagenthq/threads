---
title: "MemoryProvider"
description: "The MemoryProvider type."
icon: "plug"
---

<Note>A protocol: adapters implement it.</Note>

## Properties

<ResponseField name="writeEffect / write_effect" type={"EffectClass"}>
  The effect class of save_memory and forget_memory with this provider. Absent: unguarded. idempotent requires dedup_window_ms: the provider dedups on the key within it.
</ResponseField>

<ResponseField name="dedupWindowMs / dedup_window_ms" type={"PosInt"}>
   
</ResponseField>

## Methods

### remember

<CodeGroup>

```ts TypeScript
remember(scope: Scope, record: MemoryRecord, key: string): Promise<Result<RecordRef>>
```

```python Python
async def remember(
    scope: Scope,
    record: MemoryRecord,
    key: str,
) -> Ok[RecordRef] | Err[Failure]
```

</CodeGroup>

<ParamField body="scope" type={"Scope"} required>
  See [Scope](/reference/types/Scope).
</ParamField>

<ParamField body="record" type={"MemoryRecord"} required>
  See [MemoryRecord](/reference/types/MemoryRecord).
</ParamField>

<ParamField body="key" type={"string"} required>
   
</ParamField>

**Returns an error value** with one of these codes: `unavailable`, `timeout`, `invalid`, `scope_violation`.

### recall

<CodeGroup>

```ts TypeScript
recall(scope: Scope, query: string, options?: {
  k?: PosInt;
}): Promise<Result<readonly MemoryHit[]>>
```

```python Python
async def recall(
    scope: Scope,
    query: str,
    *,
    k: PosInt = 5,
) -> Ok[Sequence[MemoryHit]] | Err[Failure]
```

</CodeGroup>

<ParamField body="scope" type={"Scope"} required>
  See [Scope](/reference/types/Scope).
</ParamField>

<ParamField body="query" type={"string"} required>
   
</ParamField>

<ParamField body="k" type={"PosInt"} default={"5"}>
   
</ParamField>

**Returns an error value** with one of these codes: `unavailable`, `timeout`, `invalid`, `scope_violation`.

### forget

<CodeGroup>

```ts TypeScript
forget(scope: Scope, id: string, key: string): Promise<Result<void>>
```

```python Python
async def forget(
    scope: Scope,
    id: str,
    key: str,
) -> Ok[None] | Err[Failure]
```

</CodeGroup>

<ParamField body="scope" type={"Scope"} required>
  See [Scope](/reference/types/Scope).
</ParamField>

<ParamField body="id" type={"string"} required>
   
</ParamField>

<ParamField body="key" type={"string"} required>
   
</ParamField>

**Returns an error value** with one of these codes: `unavailable`, `timeout`, `invalid`, `scope_violation`.
