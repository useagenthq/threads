---
title: "SearchBackend"
description: "A web_search backend adapter (Exa, Brave, Tavily)."
icon: "plug"
---

<Note>A protocol: adapters implement it.</Note>

## Methods

### search

<CodeGroup>

```ts TypeScript
search(query: string, options?: {
  allowedDomains?: readonly string[];
  blockedDomains?: readonly string[];
}): Promise<Result<readonly SearchHit[]>>
```

```python Python
async def search(
    query: str,
    *,
    allowed_domains: Sequence[str] | None = None,
    blocked_domains: Sequence[str] | None = None,
) -> Ok[Sequence[SearchHit]] | Err[Failure]
```

</CodeGroup>

<ParamField body="query" type={"string"} required>
   
</ParamField>

<ParamField body="allowedDomains / allowed_domains" type={"readonly string[]"}>
   
</ParamField>

<ParamField body="blockedDomains / blocked_domains" type={"readonly string[]"}>
   
</ParamField>

**Returns an error value** with one of these codes: `unavailable`, `timeout`.
