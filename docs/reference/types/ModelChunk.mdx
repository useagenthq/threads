---
title: "ModelChunk"
description: "The ModelChunk type."
icon: "braces"
---

A union discriminated by `kind`.

### delta

<ResponseField name="kind" type={"\"delta\""} required>
   
</ResponseField>

<ResponseField name="text" type={"string"} required>
   
</ResponseField>

### part

<ResponseField name="kind" type={"\"part\""} required>
   
</ResponseField>

<ResponseField name="part" type={"OutputPart"} required>
   
</ResponseField>

### done

<ResponseField name="kind" type={"\"done\""} required>
   
</ResponseField>

<ResponseField name="stop_reason" type={"ModelResponseEvent.data.stop_reason"} required>
   
</ResponseField>

<ResponseField name="usage" type={"Usage"} required>
   
</ResponseField>

### rejected

<ResponseField name="kind" type={"\"rejected\""} required>
   
</ResponseField>

<ResponseField name="reason" type={"\"rate_limited\" | \"overloaded\" | \"server_error\" | \"prompt_too_long\" | \"provider_error\" | \"stale_epoch\" | \"content_unsupported\" | \"continuation_unsupported\" | \"transport_fence_unsupported\""} required>
  The terminal error of a send: exactly Model.send returns.errors. Provider classes are recorded as model_attempt_abandoned&#123;provider_outcome: failed&#125;; stale_epoch appends nothing (the writer lost its lease); content_unsupported, continuation_unsupported and transport_fence_unsupported are recorded as not_sent and end the turn with that code, never retried.
</ResponseField>

<ResponseField name="http_status" type={"number"}>
   
</ResponseField>

<ResponseField name="retry_after_ms" type={"number"}>
   
</ResponseField>

<ResponseField name="billing" type={"ModelAttemptAbandonedEvent.data.billing"}>
   
</ResponseField>
